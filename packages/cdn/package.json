{
  "name": "@venz/cdn",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "typecheck": "tsgo --noEmit -p tsconfig.json && tsgo --noEmit -p tsconfig.worker.json",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.3",
    "@hono/node-server": "^2.0.0",
    "@resvg/resvg-js": "^2.6.2",
    "@venz/shared": "workspace:*",
    "hono": "^4.12.15",
    "linkedom": "^0.18.12",
    "sharp": "^0.34.5"
  }
}
